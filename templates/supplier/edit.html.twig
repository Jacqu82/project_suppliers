{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary" formnovalidate>Edytuj</button>
    {{ form_end(form) }}

    <hr>
    <a href="{{ path('supplier_list') }}" class="btn btn-info">Wróć do listy</a>

{% endblock %}