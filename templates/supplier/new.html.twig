{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-success" formnovalidate>Dodaj</button>
    {{ form_end(form) }}

{% endblock %}