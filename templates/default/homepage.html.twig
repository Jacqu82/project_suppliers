{% extends 'base.html.twig' %}

{% block body %}

    <h3><a href="{{ path('supplier_list') }}" class="btn btn-success">Dostawcy</a></h3>
    <h3><a href="{{ path('warehouse_list') }}" class="btn btn-primary">Magazyny</a></h3>
    <h3>Źródła</h3>

{% endblock %}